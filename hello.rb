# set of very handy packages to have (remember to gem install first OR )
require "active_support/all"
require "pry-byebug"
require "better_errors"

x = gets.chomp # adding the chomp, we won't get a weird \n thing

pp x

# at terminal, typing irb you'll be able to test one line at a time
# exit by typing exit
